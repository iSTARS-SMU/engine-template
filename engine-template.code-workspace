// VSCode multi-root workspace for the full 5-stage local pipeline.
//
// Open via: File → Open Workspace from File... → pick this file.
// (Opening the engine-template folder directly only loads ONE root —
// you won't see the sibling engine sources.)
//
// Sibling folders below are shown as "missing" in the explorer until
// you `git clone` them next to engine-template; clone-then-refresh
// makes them appear. See README.md "Run a multi-stage pipeline locally"
// for the clone + pip install steps.
{
    "folders": [
        { "name": "engine-template (this engine)",  "path": "." },
        { "name": "recon-targetinfo (sibling)",     "path": "../recon-targetinfo" },
        { "name": "weakness-gather-exa (sibling)",  "path": "../weakness-gather-exa" },
        { "name": "attack-plan-llm (sibling)",      "path": "../attack-plan-llm" },
        { "name": "exploit-autoeg (sibling)",       "path": "../exploit-autoeg" },
        { "name": "report-docx-pro (sibling)",      "path": "../report-docx-pro" }
    ]
}
